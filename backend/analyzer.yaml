# relive-analyzer API 模式配置
server:
  endpoint: "http://localhost:8080"
  api_key: "sk-relive-5aa3c69c33d3d30a38cf1b817e464a37"
  timeout: 30

analyzer:
  workers: 2
  fetch_limit: 5
  retry_count: 3
  retry_delay: 5
  checkpoint_file: "./data/analyzer-checkpoint.db"

ai:
  provider: "vllm"
  vllm:
    endpoint: "http://10.222.107.22:8028"
    model: "/workspace/data/modelscope_cache/Qwen/Qwen3.5-9B"
    temperature: 0.7
    timeout: 60

download:
  temp_dir: "./data/analyzer-temp"
  timeout: 60
  max_concurrent: 3
  retry_count: 3
  keep_temp: false

batch:
  size: 5
  flush_interval: 30
  max_retry: 3
  retry_delay: 5

logging:
  level: "debug"
  console: true
  file: "./data/logs/analyzer.log"
